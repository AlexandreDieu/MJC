{% extends 'base.html.twig' %}

{% block body %}
	<main>
		<h1>Modification d'événement</h1>

		{{ include('evenement/_form.html.twig', {'button_label': 'Valider'}) }}

		<a class="btn btn-secondary m-1" href="{{ path('evenement_index') }}">Revenir à la liste</a>

		{{ include('evenement/_delete_form.html.twig') }}
	</main>
{% endblock %}
