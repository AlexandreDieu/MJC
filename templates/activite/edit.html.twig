{% extends 'base.html.twig' %}

{% block body %}
<main>
    <h1>Modifier Activité</h1>

    {{ include('activite/_form.html.twig', {'button_label': 'Valider'}) }}

    <a class="btn btn-secondary m-1" href="{{ path('activite_index') }}">Revenir à la liste</a>

    {{ include('activite/_delete_form.html.twig') }}
    </main>
{% endblock %}
