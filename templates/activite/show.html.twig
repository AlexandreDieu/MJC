{% extends 'base.html.twig' %}

{% block title %}Activite
{% endblock %}

{% block body %}
	<main>
		<h1>Activite</h1>

		<table class="table container">
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ activite.id }}</td>
				</tr>
				<tr>
					<th>Lieu</th>
					<td>{{ activite.lieu }}</td>
				</tr>
				<tr>
					<th>DateHeure</th>
					<td>{{ activite.dateHeure ? activite.dateHeure|date('Y-m-d H:i:s') : '' }}</td>
				</tr>
				<tr>
					<th>Img</th>
					<td>{{ activite.img }}</td>
				</tr>
				<tr>
					<th>Nom</th>
					<td>{{ activite.nom }}</td>
				</tr>
			</tbody>
		</table>

		<a class="btn btn-primary" href="{{ path('activite_index') }}">Revenir à la liste</a>

		<a class="btn btn-secondary" href="{{ path('activite_edit', {'id': activite.id}) }}">Modifier</a>

		{{ include('activite/_delete_form.html.twig') }}
	</main>
{% endblock %}
