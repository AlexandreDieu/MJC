{% extends 'base.html.twig' %}

{% block body %}
	<main>
		<h1>Modification adhérent</h1>

		<div>
			{{ include('adherent/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
			<a class="btn btn-secondary m-1" href="{{ path('adherent_index') }}">Revenir à la liste</a>
		</div>

		{{ include('adherent/_delete_form.html.twig') }}
	</main>
{% endblock %}
